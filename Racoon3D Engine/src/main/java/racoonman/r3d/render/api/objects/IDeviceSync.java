package racoonman.r3d.render.api.objects;

import racoonman.r3d.render.natives.IHandle;

//only used for synchronizing the GPU, so nothing going on client side
public interface IDeviceSync extends IHandle {
}
